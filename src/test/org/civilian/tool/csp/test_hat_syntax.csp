package test

template(String name, int i)
{{
	escaped hat: ^^...
	^'
	alpha^'beta
	skipln^\ignored
	^' mixed ^name^\
	pre^?today| today |post 
	pre^?{today > 0}[is ^name]post
	expr: ^name or ^{name.length()}
	stmt: ^{name = null;}
	line=^?{i.>.0}|, |^{name.charAt(i)} 
}}